{
	"name": "obsidian-polis",
	"version": "0.1.2",
	"description": "Group your vaults into contexts and switch between them in one click.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [],
	"author": "Mark <https://github.com/SevenNeumann>",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"@types/sortablejs": "^1.15.9",
		"builtin-modules": "^3.3.0",
		"esbuild": "^0.19.11",
		"obsidian": "latest",
		"tslib": "2.6.2",
		"typescript": "5.3.3"
	},
	"dependencies": {
		"sortablejs": "^1.15.7"
	}
}
